module.exports = {
    entry: './Src/Scripts/Main.js', // The entry point for your code
    output: {
      filename: 'bundle.js', // The name of the bundled file
      path: __dirname + '/Dist', // The directory where the bundled file should be saved
    },
  };
  